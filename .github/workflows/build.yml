name: Build
on: [push, pull_request]

jobs:
  build_on_linux:
    runs-on: arch-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@master
      with:
        node-version: 16
    - name: install dependencies
      run: yarn install
    - name: build
      run: yarn dist